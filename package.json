{
  "name": "invitation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Wedding invitation for Waylon and Asuka. A static three-language single page, no backend.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:static": "vite-node scripts/preview.jsx",
    "icons": "node scripts/make-icons.js",
    "contrast": "node scripts/check-contrast.js",
    "check:content": "node scripts/check-content.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
